using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitiateAreaLight : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Get the vertices from the current MeshFilter

        // Validate vertices (Only 4 vertices for now)

        // Maybe transform the vertices?

        // Send these vertices to shader

        // Set the material to emissive
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
